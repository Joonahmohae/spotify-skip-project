"""
what goes here:
- checking required columns exist
- printing shapes (rows x columns)
- generic logging helper (print step names)
- safe type conversion helpers
- small validation functions
"""

import pandas as pd

#
def check_column_type(df: pd.DataFrame, columns: str):
    raise NotImplementedError("Will implement later")